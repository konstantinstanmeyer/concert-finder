// let results = [];
// things._embedded.events.map(event => {
//   event._embedded.attractions.filter(attraction => attraction.name === "The Flaming Lips").forEach(concert => {
//   results = [...results, {
//     eventName: event._embedded.venues[0].name,
//     artist: concert.name,
//     location: event._embedded.venues[0].city.name + event._embedded.venues[0].state.stateCode,
//   }];
// })})

// console.log(results);